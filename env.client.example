# TCP-over-Nostr Client Configuration
# Copy this file to .env and modify the values as needed

# Required: Client mode
TON_MODE=client

# Client port configuration
TON_CLIENT_PORT=2222

# Required: Server's Nostr public key
# Get this from the server startup output
# Hex format: TON_SERVER_KEY=f1621517421770bccfa790bfa2a64b3798ff7adab31eaa80cd68bb646f857b38
# Npub format: TON_SERVER_KEY=npub1abc123...

# Nostr relay configuration
# Single relay
# TON_RELAY=wss://relay.damus.io

# Multiple relays (comma-separated) - Default configuration
TON_RELAY=wss://relay.damus.io,wss://relay.primal.net,wss://nostr.girino.org

# Private key (optional - will generate if not provided)
# Hex format: TON_PRIVATE_KEY=4c2800f5a0a4fb6d09afce6ec470f09f29250abe09e6558029fad0691c857721
# Nsec format: TON_PRIVATE_KEY=nsec1abc123...

# Logging
TON_VERBOSE=true

# Examples for different use cases:

# SSH Client (connects to SSH server via Nostr)
# TON_MODE=client
# TON_CLIENT_PORT=2222
# TON_SERVER_KEY=npub1abc123...
# TON_RELAY=wss://relay.damus.io
# TON_VERBOSE=true
# Usage: ssh -p 2222 user@localhost

# HTTP Client (connects to HTTP server via Nostr)
# TON_MODE=client
# TON_CLIENT_PORT=8080
# TON_SERVER_KEY=npub1abc123...
# TON_RELAY=wss://relay.damus.io,wss://relay.snort.social
# TON_VERBOSE=false
# Usage: curl http://localhost:8080

# Database Client (connects to database via Nostr)
# TON_MODE=client
# TON_CLIENT_PORT=5432
# TON_SERVER_KEY=npub1abc123...
# TON_RELAY=wss://relay.damus.io
# TON_VERBOSE=true
# Usage: psql -h localhost -p 5432 -U username database
