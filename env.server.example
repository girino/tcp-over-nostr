# TCP-over-Nostr Server Configuration
# Copy this file to .env and modify the values as needed

# Required: Server mode
TON_MODE=server

# Target server configuration
# Option 1: Separate host and port
TON_TARGET_HOST=httpbin.org
TON_TARGET_PORT=80

# Option 2: Combined host:port format (overrides separate values)
# TON_TARGET_HOST=httpbin.org:80

# Nostr relay configuration
# Single relay
# TON_RELAY=wss://relay.damus.io

# Multiple relays (comma-separated) - Default configuration
TON_RELAY=wss://relay.damus.io,wss://relay.primal.net,wss://nostr.girino.org

# Private key (optional - will generate if not provided)
# Hex format: TON_PRIVATE_KEY=4c2800f5a0a4fb6d09afce6ec470f09f29250abe09e6558029fad0691c857721
# Nsec format: TON_PRIVATE_KEY=nsec1abc123...

# Logging
TON_VERBOSE=true

# Examples for different use cases:

# SSH Proxy Server
# TON_MODE=server
# TON_TARGET_HOST=192.168.1.100:22
# TON_RELAY=wss://relay.damus.io
# TON_VERBOSE=true

# HTTP Proxy Server (Default configuration)
# TON_MODE=server
# TON_TARGET_HOST=httpbin.org:80
# TON_RELAY=wss://relay.damus.io,wss://relay.primal.net,wss://nostr.girino.org
# TON_VERBOSE=true

# Database Proxy Server
# TON_MODE=server
# TON_TARGET_HOST=localhost:5432
# TON_RELAY=wss://relay.damus.io
# TON_VERBOSE=true
